<nav class="navbar navbar-expand-lg fixed-top navbar-dark shadow box-shadow bg-dark py-2">
    <div class="container-fluid">
       

            
            
        

        <a class="navbar-brand text-danger fs-4" href="#"><img src="~/images/favicon.png" style="width:40px;" alt="Resturant-logo"> Restruant<span class="text-light">.</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav flex-grow-1">
                <li class="nav-item">
                    <a class="nav-link text-light" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light" asp-area="" asp-controller="product" asp-action="GetAllMeals">Product</a>
                </li>
                @if (User.Identity.IsAuthenticated == true && User.IsInRole("Admin"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-light" asp-area="" asp-controller="Book" asp-action="Getall">Orders</a>
                </li>
                }
            </ul>

            <div class="d-flex">
                
                <a class="btn btn-danger d-none me-2  " id="btnbuy" asp-action="Save" asp-controller="Book">Buy</a>
                @if(!User.IsInRole("Admin")){
                 <a class="link-light fs-4 me-5" asp-controller="Book" asp-action="userBook"><i class="fa-solid fa-shopping-cart"></i></a> 
                }

                @if (User.Identity.IsAuthenticated == false)
                {
                    <a class="btn btn-outline-danger" asp-controller="Acount" asp-action="Login">Log in</a>
                }
                else
                {
                    <a class="btn btn-outline-danger" asp-controller="Acount" asp-action="Signout">Log out</a>

                }
            </div>
        </div>
    </div>
</nav>